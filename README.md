# README

## Getting started

#### Install dependencies

Run `npm install`.

#### Development

Run `npm run dev`

#### Development with liveserver + hmr

Run `npm run watch`

#### Production

Run `npm run build`
