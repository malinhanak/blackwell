# README

## Getting started

#### Install dependencies

Run `npm install`.

#### Development

Run `npm run dev`

#### Development with devserver + hmr

Run `npm run devserver`

#### Production

Run `npm run build`
